extends layout

block content
    div.text-center
        form(class="form-signin" method="POST" action="/")
            #error
                if error
                    p.text-danger Wrong login or password!

            - var h1Classes = ['h3', 'mb-3', 'font-weight-normal']
            h1(class=h1Classes) Please sign in

            label( for="inputEmail" class="sr-only") Login
            input(name="login" id="inputLogin" class="form-control" placeholder="Login: Monochrome" required autofocus)

            label(for="inputPassword" class="sr-only") Password
            input(type="password" name="password" id="inputPassword" class="form-control" placeholder="Password: 111" required)

            - var buttonClass=['btn', 'btn-lg', 'btn-primary', 'btn-block'];
            button(class=buttonClass type="submit") Sign in